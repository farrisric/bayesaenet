# @package _global_

defaults:
  - /hpsearch/study: study
  - /hpsearch/objective: bnn
  - override /experiment: bnn_lrt

hpsearch:
  monitor: elbo/val
  study:
    study_name: bnn_lrt

trainer:
  max_epochs: 20

callbacks:
  early_stopping: null
