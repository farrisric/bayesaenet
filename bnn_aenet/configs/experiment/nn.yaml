# @package _global_

defaults:
#  - override /datamodule: datamodule
  - override /model: nn
  - override /callbacks: default
  - override /trainer: default

seed: 12345

trainer:
  min_epochs: 1
  max_epochs: 5

callbacks:
  model_checkpoint:
    monitor: rmse/val

  early_stopping:
    monitor: rmse/val
    patience: 20