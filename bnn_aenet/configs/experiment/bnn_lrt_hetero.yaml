# @package _global_

defaults:
  - override /model: bnn_hetero
  - override /callbacks: default
  - override /trainer: default

tags: ["bayesian", "lrt", "heteroscedastic"]

seed: 12345

trainer:
  min_epochs: 5000
  max_epochs: 50000

callbacks:
  model_checkpoint:
    monitor: total_rmse/val
  early_stopping:
    monitor: total_rmse/val
    patience: 100

model:
  name: lrt_hetero
  fit_context: lrt
  guide: normal
  net:
    alpha: 0.1
