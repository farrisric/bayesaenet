# @package _global_

defaults:
  - /hpsearch/study: study
  - /hpsearch/objective: bnn
  - override /experiment: hom_rad

hpsearch:
  monitor: elbo/val
  study:
    study_name: hom_rad

trainer:
  max_epochs: 20

callbacks:
  early_stopping: null
