# @package _global_

defaults:
  - /hpsearch/study: study
  - /hpsearch/objective: bnn
  - override /experiment: bnn_lrt

hpsearch:
  monitor: mse/val
  study:
    study_name: bnn_lrt

trainer:
  max_epochs: 5000

callbacks:
  early_stopping:
    monitor: mse/val
    patience: 50
